*** Begin Patch
*** Update File: app/api/chat/route.ts
@@
 function containsAny(
   text: string,
   patterns: string[]
 ) {
   return patterns.some(
     (pattern) =>
       text.includes(pattern)
   );
 }

+function isMemoryRecallQuestion(
+  message: string
+) {
+  const normalized =
+    message.replace(/\s+/g, "");
+
+  return containsAny(
+    normalized,
+    [
+      "覚えてる",
+      "覚えている",
+      "覚えてた",
+      "何覚えてる",
+      "なに覚えてる",
+      "何を覚えてる",
+      "なにを覚えてる",
+      "記憶してる",
+      "記憶している",
+      "私のこと覚えて",
+      "俺のこと覚えて",
+      "僕のこと覚えて",
+      "好み覚えて",
+      "何知ってる",
+      "なに知ってる",
+    ]
+  );
+}
+
@@
     const safeRelationshipPoints =
       typeof relationshipPoints ===
         "number" &&
       Number.isFinite(
         relationshipPoints
@@
         : userMessageCount;

+    const memoryRecallMode =
+      isMemoryRecallQuestion(
+        message
+      );
+
     const relationshipGuide =
       createRelationshipGuide(
         safeRelationshipPoints
       );

     const userProfile =
       buildUserProfile(
-        [
-          ...safeHistory,
-          {
-            role: "user",
-            text: message,
-          },
-        ],
+        memoryRecallMode
+          ? [
+              {
+                role: "user",
+                text: message,
+              },
+            ]
+          : [
+              ...safeHistory,
+              {
+                role: "user",
+                text: message,
+              },
+            ],
         safeMemory
       );
@@
     const activityEvidence =
       buildActivityEvidence(
-        safeHistory,
+        memoryRecallMode
+          ? []
+          : safeHistory,
         safeTodayMemory
       );
@@
     const recentMisakiMessages =
-      safeHistory
-        .filter(
-          (item) =>
-            item.role === "misaki"
-        )
-        .slice(-8)
-        .map(
-          (item) => item.text
-        );
+      memoryRecallMode
+        ? []
+        : safeHistory
+            .filter(
+              (item) =>
+                item.role === "misaki"
+            )
+            .slice(-8)
+            .map(
+              (item) => item.text
+            );
@@
     const weatherHistoryGuide =
-      hasRecentMisakiWeatherContext(
-        safeHistory
-      )
+      !memoryRecallMode &&
+      hasRecentMisakiWeatherContext(
+        safeHistory
+      )
@@
     const contents =
-      safeHistory.map(
-        (item) => ({
-          role:
-            item.role === "user"
-              ? "user"
-              : "model",
-          parts: [
-            {
-              text: item.text,
-            },
-          ],
-        })
-      );
+      memoryRecallMode
+        ? []
+        : safeHistory.map(
+            (item) => ({
+              role:
+                item.role === "user"
+                  ? "user"
+                  : "model",
+              parts: [
+                {
+                  text: item.text,
+                },
+              ],
+            })
+          );
@@
 【長期記憶】

 ${memoryText}

 必要なときだけ自然に使ってください。

+${memoryRecallMode
+  ? `
+【記憶確認モード】
+
+今回はユーザーが、
+美咲が「現在」何を覚えているかを確認しています。
+
+この返答では上の【長期記憶】だけを、
+ユーザーについて覚えている事実の根拠にしてください。
+
+会話履歴は回答コンテキストから除外されています。
+
+長期記憶にない情報は、
+「今は覚えていない」
+「今の記憶には残っていない」
+と自然に答えてください。
+
+削除済み・訂正済みの具体的内容を復元しないでください。
+また、
+「前に忘れてと言われた」
+「さっき忘れろと言った」
+など、削除した出来事自体も持ち出さないでください。
+
+ユーザーが現在のメッセージ内で具体名を提示していない限り、
+長期記憶にない具体名を推測・再掲しないでください。
+`.trim()
+  : ""}
+
 【今日の記憶に保存するルール】
*** End Patch
