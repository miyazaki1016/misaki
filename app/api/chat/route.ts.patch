*** Begin Patch
*** Update File: app/api/chat/route.ts
@@
     const safeMemory:
       string[] =
       Array.isArray(memory)
@@
             .filter(Boolean)
             .slice(-MAX_MEMORY)
         : [];
+
+    // Supabase is the canonical long-term memory.
+    // Browser/localStorage memory is only a cache/fallback.
+    const {
+      data: serverConversationState,
+      error: serverConversationStateError,
+    } =
+      await supabase
+        .from(
+          "misaki_user_conversation_state"
+        )
+        .select("memory")
+        .eq(
+          "user_id",
+          userData.user.id
+        )
+        .maybeSingle();
+
+    if (
+      serverConversationStateError
+    ) {
+      console.error(
+        "SERVER MEMORY LOAD ERROR:",
+        serverConversationStateError
+      );
+    }
+
+    const serverMemory:
+      string[] =
+      Array.isArray(
+        serverConversationState?.memory
+      )
+        ? serverConversationState.memory
+            .filter(
+              (item: unknown) =>
+                typeof item ===
+                "string"
+            )
+            .map(
+              (item: string) =>
+                item.trim()
+            )
+            .filter(Boolean)
+            .slice(-MAX_MEMORY)
+        : [];
+
+    const canonicalMemory =
+      serverConversationState &&
+      !serverConversationStateError
+        ? serverMemory
+        : safeMemory;
@@
     const userProfile =
       buildUserProfile(
         [
@@
           },
         ],
-        safeMemory
+        canonicalMemory
       );
@@
     const memoryText =
-      safeMemory.length > 0
-        ? safeMemory
+      canonicalMemory.length > 0
+        ? canonicalMemory
             .map(
               (item) =>
                 `・${item}`
@@
     const updatedMemory =
       Array.isArray(
         parsed.memory
@@
             .filter(Boolean)
             .slice(-MAX_MEMORY)
-        : safeMemory;
+        : canonicalMemory;
@@
     const updatedTodayMemory:
       MisakiTodayMemory = {
@@
         ),
     };
+
+    // Persist the canonical memory from the chat API itself.
+    // This removes localStorage from the source-of-truth path.
+    const canonicalHistory =
+      [
+        ...safeHistory,
+        {
+          role: "user" as const,
+          text: message,
+        },
+        {
+          role: "misaki" as const,
+          text: reply,
+        },
+      ].slice(-MAX_HISTORY);
+
+    const {
+      error: memoryPersistError,
+    } =
+      await (
+        supabase.rpc as any
+      )(
+        "sync_user_conversation_state",
+        {
+          p_history:
+            canonicalHistory,
+          p_memory:
+            updatedMemory,
+        }
+      );
+
+    if (memoryPersistError) {
+      console.error(
+        "SERVER MEMORY SAVE ERROR:",
+        memoryPersistError
+      );
+    }

     return Response.json({
*** End Patch
